{
  "name": "mandala",
  "private": true,
  "scripts": {
    "start": "bundle exec jekyll serve --ssl-key .ssl/key.pem --ssl-cert .ssl/cert.pem",
    "start:http": "bundle exec jekyll serve --livereload",
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  },
  "dependencies": {
    "sprae": "^12.4.10"
  }
}
